from setuptools import setup, find_packages

setup(
    name="weather-helper",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "tkinter",  # Usually comes with Python
    ],
)
